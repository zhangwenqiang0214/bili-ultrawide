让 B 站在带鱼屏 / 超宽屏上不再浪费左右两侧的空间。

B 站的内容区宽度是**写死**的：首页和分区页封顶 1980px、热门页 1286px、动态页 1330px、播放页 2560px，顶栏也是 2560px 居中。在 3440px 宽的屏幕上，两侧各有 500~900px 完全空着。本脚本解除这些上限，并按窗口宽度自动计算每行放几张卡片。

## 效果（实测于 3440×1440）

| 页面 | 原生 | 装了脚本 |
| --- | --- | --- |
| 首页 | 5 列 / 内容区 1980px | **8 列 / 铺满 3425px** |
| 分区页（/c/tech 这类） | 5 列 / 1980px | **8 列 / 铺满** |
| 热门页（含每周必看 / 入站必刷 / 排行榜） | 2 列 / 1286px | **5 列 / 铺满** |
| 动态页 | 1 列 / 1330px，左右侧栏各占一整列空高度 | **4 列瀑布流**，侧栏模块收成 183px 高的顶部信息条 |
| 播放页 | 播放器 1694×953，右侧是弹幕列表+推荐 | **播放器 2027×1140**，右栏变成 1254px 宽的**评论区** |
| 顶栏（所有页面） | 2560px 居中 | **铺满** |

均实测无横向滚动条。播放页上标题+播放器+工具栏底部落在 1435px，1440 高的屏幕一屏正好装下。

顶栏在**所有带它的页面**上都会拉满，包括播放页、动态页、搜索页。搜索页这类未适配的页面**只拉顶栏**，页面内容保持原样不动。

## 可调参数

都在文件开头，一般只需要动第一个：

- `TARGET_CARD_WIDTH = 380` —— 首页/分区页卡片目标宽度（380→8列，340→9列，300→10列）
- `POPULAR_CARD_WIDTH = 638` —— 热门页横条卡片目标宽度
- `DYNAMIC_MAX_COLUMNS = 4` —— 动态页最多几列（瀑布流排布，卡片紧跟上一张，不按横行对齐）
- `PLAYER_RATIO = 0.62` —— 播放页播放器占内容区比例，剩下的全给评论区
- `HIDE_VIDEO_POD = false` —— 播放页是否连「合集 / 分P 列表」也隐藏（默认保留，否则多P视频没有选集入口）
- `SIDE_PADDING = 60` —— 页面左右留白，不要小于 60

## 说明

- 需要支持 CSS `:has()` 的浏览器：Chrome / Edge 105+、Safari 15.4+、Firefox 121+
- 按**页面结构**识别，不用网址白名单，所以分区页和热门页的各个子标签页会自动覆盖
- 窗口窄于 1700px 时动态页自动退回 B 站原来的三栏；窄于 2600px 时播放页完全不改动
- 播放页的网页全屏 / 全屏模式下，脚本的播放器尺寸规则会自动让开
- 源码、更新和详细技术说明：https://github.com/zhangwenqiang0214/bili-ultrawide
- MIT 许可证
